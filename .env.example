# Server
PORT=3001
HOST_PIN=
# DATABASE_PATH=   # optional override; leave unset for server/data/party-queue.db
SONG_FOLDERS=F:\Games\Songs

# Production: serve built client from API
NODE_ENV=production

# Overlay (Electron)
CLIENT_URL=http://localhost:5173/overlay
OVERLAY_CLICK_THROUGH=true

# Client (optional — if API is on another host)
# VITE_API_URL=http://192.168.1.10:3001
